{% extends 'base.html.twig' %}

{% block title %}TypeReclamation{% endblock %}

{% block body %}
    <h1>TypeReclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_reclamation.id }}</td>
            </tr>
            <tr>
                <th>Type_reclamation</th>
                <td>{{ type_reclamation.typeReclamation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_reclamation_controller_php_index') }}">back to list</a>

    <a href="{{ path('app_type_reclamation_controller_php_edit', {'id': type_reclamation.id}) }}">edit</a>

    {{ include('type_reclamation_controller_php/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Gestion des Réclamations{% endblock %}

{% block stylesheets %}
<style>
  .reclamation-index-container {
    max-width: 1200px;
    margin: 40px auto;
    padding: 20px;
    background-color: #ffffff;
    border-radius: 12px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
  }

  h1 {
    text-align: center;
    color: #2c3e50;
    margin-bottom: 30px;
    font-size: 28px;
    font-weight: 600;
  }

  .table-responsive {
    border-radius: 12px;
    overflow: hidden;
  }

  .table {
    width: 100%;
    border-collapse: collapse;
  }

  .table th {
    background-color: #00B4D8;
    color: white;
    font-weight: 500;
    text-align: center;
  }

  .table td {
    text-align: center;
    vertical-align: middle;
    padding: 12px;
  }

  .table tr:hover {
    background-color: #f9f9f9;
  }

  .table td img {
    max-width: 80px;
    border-radius: 5px;
  }

  .actions a {
    display: inline-block;
    padding: 6px 12px;
    border-radius: 5px;
    text-decoration: none;
    color: white;
    background-color: #00B4D8;
    transition: 0.3s ease;
  }

  .actions a:hover {
    background-color: #0099b3;
    transform: scale(1.05);
  }

  .create-new-link {
    display: block;
    width: fit-content;
    margin: 20px auto;
    padding: 12px 24px;
    background-color: #00B4D8;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-size: 16px;
    transition: 0.3s ease;
  }

  .create-new-link:hover {
    background-color: #0099b3;
    transform: scale(1.05);
  }

  .no-records {
    text-align: center;
    color: #888;
    font-style: italic;
    padding: 20px;
  }
</style>
{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="reclamation-index-container bg-light p-4 rounded shadow">
        <h1> Liste des Réclamations</h1>

        <div class="d-flex justify-content-between align-items-center mb-3 flex-wrap">
            <span class="text-muted fs-5">
                Total Réclamations: <strong class="text-dark">{{ reclamations|length }}</strong>
            </span>

            <form action="{{ path('app_reclamation_controller_php_index') }}" method="get" class="d-flex align-items-center">
                <label for="typeReclamationFilter" class="form-label me-2 fw-bold">Filtrer par Type:</label>
                <select name="typeReclamation" id="typeReclamationFilter" class="form-select w-auto" onchange="this.form.submit()">
                    <option value="">Tous les types</option>
                    {% for typeReclamation in typesReclamation %}
                        <option value="{{ typeReclamation.id }}" {% if selectedType == typeReclamation.id %}selected{% endif %}>
                            {{ typeReclamation.typeReclamation }}
                        </option>
                    {% endfor %}
                </select>
            </form>
        </div>
    <table class="table">
        <thead>
            <tr>
                <th>Description</th>
                <th>Date Réclamation</th>
                <th>Médecin</th>
                <th>Photo</th>
                <th>Type Réclamation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for reclamation in reclamations %}
                <tr>
                    <td>{{ reclamation.description }}</td>
                    <td>{{ reclamation.dateReclamation ? reclamation.dateReclamation|date('Y-m-d H:i:s') : '' }}</td>
                    <td>{{ reclamation.medecin ? reclamation.medecin.nom : 'N/A' }}</td> {# Utilisation de medecin.nom #}
                    <td>
                        {% if reclamation.photo %}
                            <img src="{{ asset('upload/images/' ~ reclamation.photo) }}" alt="Photo" style="max-width: 100px;">
                        {% else %}
                            N/A
                        {% endif %}
                    </td>
                    <td>{{ reclamation.typeReclamation ? reclamation.typeReclamation.typeReclamation : 'N/A' }}</td>
                    <td class="actions">
                        <a href="{{ path('app_reclamation_controller_php_show', {'id': reclamation.id}) }}">Voir</a>
                        <a href="{{ path('app_reclamation_controller_php_edit', {'id': reclamation.id}) }}">Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="no-records">Aucune réclamation trouvée</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_controller_php_new') }}" class="create-new-link">Créer une nouvelle réclamation</a>
</div>

{% endblock %}

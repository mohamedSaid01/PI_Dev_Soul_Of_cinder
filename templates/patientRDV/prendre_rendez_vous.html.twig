{# templates/patient/prendre_rendez_vous.html.twig #}
{% extends 'baseFrontOffice.html.twig' %}

{% block title %}Prendre un Rendez-Vous{% endblock %}

{% block body %}
<div class="content-container">
    <h2 class="page-title">Prendre un Rendez-Vous</h2>
    
    {% for flashMessage in app.flashes('error') %}
        <div class="alert alert-danger">{{ flashMessage }}</div>
    {% endfor %}

    <div class="card mb-4">
        <div class="card-header">
            <strong>Médecin :</strong> {{ medecin.getUser().getLastName() }} {{ medecin.getUser().getFirstName() }}
        </div>
        <div class="card-body">
            {{ form_start(form) }}
            <div class="mb-3">
                {{ form_label(form.date) }}
                {{ form_widget(form.date, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.date) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.heure) }}
                {{ form_widget(form.heure, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.heure) }}
            </div>
            
            <div class="mb-3">
                {{ form_label(form.etat) }}
                {{ form_widget(form.etat, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.etat) }}
            </div>
            
            <button type="submit" class="btn btn-primary">Confirmer</button>
            <a href="{{ path('liste_medecins') }}" class="btn btn-outline-secondary">Retour</a>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}